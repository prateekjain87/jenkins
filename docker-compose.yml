version: "2"

services:
  jenkins:
    build: . 
    image: prateekjain87/jenkins
    ports:
      - "8080:8080"
    volumes:
      - ./jenkins-data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - $HOME:/home
